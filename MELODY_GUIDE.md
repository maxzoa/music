# Угадай Мелодию - Читерское приложение для квизов

Веб-приложение для Android, которое распознает музыку и дает подсказки через вибросигналы.

## Возможности

- 🎤 **Запись аудио** через микрофон (Web Audio API) - 15 секунд автоматически
- 🎵 **Распознавание музыки** через AudD API
- 📱 **Умные вибросигналы** для подсказок:
  - 1 вибро = английское название, 2 вибро = русское
  - Для каждой буквы: номер группы + номер в группе
- 🔗 **Автозапуск** по ссылке для MacroDroid
- 📊 **История** распознаваний в MongoDB
- 🎨 **Адаптивный дизайн** для мобильных устройств

## Как использовать

1. Откройте приложение на Android в Chrome/Firefox
2. Разрешите доступ к микрофону
3. Нажмите на иконку микрофона (или откройте с параметром `?autostart=true`)
4. Запись автоматически остановится через 15 секунд
5. Получите название песни и вибросигналы с подсказкой

## Автозапуск через MacroDroid

Используйте эту ссылку в MacroDroid для автоматического запуска записи:
```
https://your-app-url.com/?autostart=true
```

При открытии этой ссылки запись начнется автоматически.

## Алгоритм вибросигналов

### 1. Блок языка (первый)
- **1 вибро** (300мс) = английский
- **2 вибро** (300мс + 250мс + 300мс) = русский
- Пауза 1500мс

### 2. Блоки букв (повторяется для каждой буквы)

Каждая буква состоит из двух блоков:

**Блок 1 - Номер группы:**
- N коротких вибро (250мс каждый)
- Пауза 400мс

**Блок 2 - Номер буквы в группе:**
- M коротких вибро (250мс каждый)
- Пауза 1000мс перед следующей буквой

### Группировка букв

**Русский** (7 групп по 5 букв):
1. **А-Д**: А, Б, В, Г, Д
2. **Е-Й**: Е, Ё, Ж, З, И
3. **Й-Н**: Й, К, Л, М, Н
4. **О-Т**: О, П, Р, С, Т
5. **У-Ч**: У, Ф, Х, Ц, Ч
6. **Ш-Ь**: Ш, Щ, Ъ, Ы, Ь
7. **Э-Я**: Э, Ю, Я

**Английский** (6 групп):
1. **A-E**: A, B, C, D, E
2. **F-J**: F, G, H, I, J
3. **K-O**: K, L, M, N, O
4. **P-T**: P, Q, R, S, T
5. **U-Y**: U, V, W, X, Y
6. **Z**: Z

### Пример: "ЗАТ" (Заточка)

**Полный паттерн:**
```
1. ЯЗЫК: 2 вибро → РУССКИЙ
2. З: 2 вибро (группа 2: Е-Й) + 4 вибро (4-я буква: З)
3. А: 1 вибро (группа 1: А-Д) + 1 вибро (1-я буква: А)
4. Т: 4 вибро (группа 4: О-Т) + 5 вибро (5-я буква: Т)
```

**Краткая запись:**
`2(RU) → 2+4(З) → 1+1(А) → 4+5(Т)`

## Настройка

### Backend (.env)
```
AUDD_API_TOKEN=your_audd_token_here
MONGO_URL=mongodb://localhost:27017
DB_NAME=melody_guesser
```

### Получение AudD токена
1. Зарегистрируйтесь на https://audd.io
2. Получите бесплатный API токен (300 запросов/месяц)
3. Добавьте токен в `/app/backend/.env`
4. Перезапустите backend: `sudo supervisorctl restart backend`

## Технологии

- **Frontend**: React, Web Audio API, Vibration API, Tailwind CSS, shadcn/ui
- **Backend**: FastAPI, Python, AudD API
- **Database**: MongoDB
- **API**: AudD Music Recognition API (бесплатный tier: 300 запросов/месяц)

## API Endpoints

- `GET /api/` - проверка API
- `POST /api/recognize` - распознавание музыки (multipart/form-data)
- `GET /api/status` - история распознаваний
- `POST /api/status` - создать запись статуса

## Ограничения

- Web Audio API требует HTTPS или localhost
- Vibration API работает только на Android (Chrome, Firefox)
- AudD API: 300 бесплатных запросов/месяц
- Максимальное время записи: 15 секунд (автоматическая остановка)
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: MP3, WAV, WebM, OGG

## Тестирование алгоритма

```bash
# Тест для слова "ЗАТ"
python3 test_vibration.py
```

Результат для "ЗАТ":
- Буква З: группа 2 (Е-Й), позиция 4 → 2 вибро + 4 вибро
- Буква А: группа 1 (А-Д), позиция 1 → 1 вибро + 1 вибро  
- Буква Т: группа 4 (О-Т), позиция 5 → 4 вибро + 5 вибро

## Next Action Items

- Получить API ключ AudD и добавить в `/app/backend/.env`
- Протестировать на реальном Android устройстве
- Проверить работу вибросигналов в реальных условиях
- Настроить MacroDroid с ссылкой автозапуска
